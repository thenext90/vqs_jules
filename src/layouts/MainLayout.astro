---
const { title } = Astro.props;
---
<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-blue: #0066CC;
            --primary-blue-dark: #004499;
            --secondary-green: #00B894;
            --accent-orange: #FF6B35;
            --neutral-100: #F8FAFC;
            --neutral-200: #E2E8F0;
            --neutral-300: #CBD5E1;
            --neutral-400: #94A3B8;
            --neutral-500: #64748B;
            --neutral-600: #475569;
            --neutral-700: #334155;
            --neutral-800: #1E293B;
            --neutral-900: #0F172A;
            --white: #FFFFFF;
            --gradient-primary: linear-gradient(135deg, var(--primary-blue) 0%, var(--secondary-green) 100%);
            --gradient-secondary: linear-gradient(135deg, var(--neutral-100) 0%, var(--white) 100%);
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
            --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: var(--neutral-100);
            color: var(--neutral-800);
            line-height: 1.6;
            font-size: 16px;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background: var(--white);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--neutral-200);
            position: sticky;
            top: 0;
            z-index: 100;
            transition: all 0.3s ease;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 1rem 2rem;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary-blue);
            text-decoration: none;
            transition: transform 0.2s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background: var(--gradient-primary);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 1.2rem;
        }

        nav {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        nav a {
            color: var(--neutral-700);
            text-decoration: none;
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            transition: all 0.2s ease;
            position: relative;
        }

        nav a:hover {
            color: var(--primary-blue);
            background-color: var(--neutral-100);
        }

        nav a.active {
            color: var(--primary-blue);
            background-color: var(--neutral-100);
        }

        .cta-button {
            background: var(--gradient-primary);
            color: white !important;
            padding: 0.75rem 1.5rem !important;
            border-radius: 8px;
            font-weight: 600;
            box-shadow: var(--shadow-md);
            transition: all 0.2s ease;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
            background: var(--primary-blue-dark) !important;
        }

        main {
            flex: 1;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
            padding: 0 2rem;
        }

        footer {
            background: var(--neutral-900);
            color: var(--neutral-300);
            margin-top: 4rem;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 3rem 2rem 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .footer-section h3 {
            color: var(--white);
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .footer-section p, .footer-section a {
            color: var(--neutral-400);
            text-decoration: none;
            line-height: 1.6;
        }

        .footer-section a:hover {
            color: var(--secondary-green);
        }

        .footer-bottom {
            border-top: 1px solid var(--neutral-800);
            padding: 1.5rem 2rem;
            text-align: center;
            color: var(--neutral-500);
        }

        .mobile-menu-toggle {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--neutral-700);
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .header-content {
                padding: 1rem;
            }

            .mobile-menu-toggle {
                display: block;
            }

            nav {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                right: 0;
                background: var(--white);
                border-top: 1px solid var(--neutral-200);
                flex-direction: column;
                padding: 1rem;
                gap: 0.5rem;
            }

            nav.active {
                display: flex;
            }

            nav a {
                width: 100%;
                text-align: center;
                padding: 1rem;
            }

            main {
                padding: 0 1rem;
            }

            .footer-content {
                padding: 2rem 1rem 1rem;
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .footer-bottom {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="header-content">
            <a href="/" class="logo">
                <div class="logo-icon">AI</div>
                <span>VQS</span>
            </a>
            <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">☰</button>
            <nav id="nav-menu">
                <a href="/">Productos</a>
                <a href="/nosotros">Sobre Nosotros</a>
                <a href="/servicios">Servicios</a>
                <a href="/contacto" class="cta-button">Contacto</a>
            </nav>
        </div>
    </header>
    
    <main>
        <slot />
    </main>
    
    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>VQS - Certificación IA</h3>
                <p>Liderando la certificación de productos alimentarios mediante inteligencia artificial avanzada. Garantizamos calidad, seguridad y confianza en cada producto certificado.</p>
            </div>
            <div class="footer-section">
                <h3>Servicios</h3>
                <p><a href="/servicios">Certificación de Alimentos</a></p>
                <p><a href="/servicios">Análisis de Calidad IA</a></p>
                <p><a href="/servicios">Auditorías Automatizadas</a></p>
                <p><a href="/servicios">Monitoreo Continuo</a></p>
            </div>
            <div class="footer-section">
                <h3>Contacto</h3>
                <p>Email: info@vqs-certification.com</p>
                <p>Teléfono: +56 2 2345 6789</p>
                <p>Dirección: Santiago, Chile</p>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 VQS - Verification Quality Service. Todos los derechos reservados.</p>
        </div>
    </footer>

    <script>
        function toggleMobileMenu() {
            const nav = document.getElementById('nav-menu');
            nav.classList.toggle('active');
        }

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
            const nav = document.getElementById('nav-menu');
            const toggle = document.querySelector('.mobile-menu-toggle');
            
            if (!nav.contains(event.target) && !toggle.contains(event.target)) {
                nav.classList.remove('active');
            }
        });

        // Add scroll effect to header
        window.addEventListener('scroll', function() {
            const header = document.querySelector('header');
            if (window.scrollY > 10) {
                header.style.boxShadow = 'var(--shadow-md)';
            } else {
                header.style.boxShadow = 'none';
            }
        });
    </script>
</body>
</html>
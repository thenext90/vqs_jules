---
import MainLayout from '../layouts/MainLayout.astro';

const teamMembers = [
    {
        name: 'Dr. María González',
        position: 'CEO & Fundadora',
        specialty: 'Análisis de Calidad de Procesos',
        image: 'https://images.unsplash.com/photo-1559839734-2b71ea197ec2?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80',
        experience: '20+ años',
        education: 'PhD en Gestión de Calidad'
    },
    {
        name: 'Ing. Carlos Ruiz',
        position: 'CTO',
        specialty: 'Tecnologías de Verificación',
        image: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=687&q=80',
        experience: '15+ años',
        education: 'MS en Ciencia de Datos'
    },
    {
        name: 'Dra. Ana Martínez',
        position: 'Directora de Cumplimiento',
        specialty: 'Regulaciones y Normativas',
        image: 'https://images.unsplash.com/photo-1551836022-d5d88e9218df?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=687&q=80',
        experience: '18+ años',
        education: 'Doctorado en Derecho Regulatorio'
    },
    {
        name: 'Ing. Roberto Silva',
        position: 'Director de Operaciones',
        specialty: 'Optimización de la Cadena de Valor',
        image: 'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80',
        experience: '16+ años',
        education: 'MBA en Operaciones'
    }
];

const milestones = [
    {
        year: '2018',
        title: 'Nace la Idea',
        description: 'Un grupo de expertos identifica la creciente brecha entre lo que las empresas prometen y lo que los clientes reciben.',
        icon: '💡'
    },
    {
        year: '2019',
        title: 'Fundación de VQS',
        description: 'Se funda VQS con la misión de ser el verificador de confianza del mercado, comenzando con el sector alimentario.',
        icon: '🚀'
    },
    {
        year: '2021',
        title: 'Expansión de Servicios',
        description: 'Lanzamiento de las divisiones de Seguridad de la Información y Verificación de Mantenimiento.',
        icon: '🔧'
    },
    {
        year: '2022',
        title: 'Plataforma de Análisis',
        description: 'Inauguramos nuestra área de Encuestas y Estadísticas para medir la satisfacción del cliente de forma precisa.',
        icon: '📊'
    },
    {
        year: '2023',
        title: 'Consolidación Global',
        description: 'VQS se establece como un referente en la verificación de la calidad en múltiples industrias y mercados.',
        icon: '🌍'
    },
    {
        year: 'Hoy',
        title: 'El Futuro de la Certeza',
        description: 'Continuamos innovando para que cada interacción en el mercado sea transparente, justa y satisfactoria.',
        icon: '✅'
    }
];

const principles = [
    { name: 'Foco en el Cliente', description: 'Las necesidades del cliente son el centro de todos nuestros procesos de verificación.', icon: '🎯' },
    { name: 'Cumplimiento de Plazos', description: 'Verificamos que las entregas y servicios se realicen en la fecha y hora comprometidas.', icon: '⏱️' },
    { name: 'Calidad Garantizada', description: 'Aseguramos que el producto o servicio recibido es exactamente lo que se compró.', icon: '⭐' },
    { name: 'Precio Respetado', description: 'Validamos que los precios y condiciones acordadas se mantengan sin sorpresas.', icon: '💰' },
    { name: 'Certeza como Derecho', description: 'Creemos que tener certeza sobre lo que se adquiere es un derecho fundamental del consumidor.', icon: '⚖️' },
    { name: 'Transparencia Total', description: 'Fomentamos la transparencia en la cadena de valor para fortalecer la confianza.', icon: '🔗' }
];
---

<MainLayout title="VQS - Sobre Nosotros | Dando Certeza al Mercado">
    <!-- Hero Section -->
    <section class="about-hero">
        <div class="hero-background">
            <div class="hero-pattern"></div>
            <div class="hero-glow"></div>
        </div>
        <div class="hero-content fade-in-up">
            <div class="hero-text">
                <div class="hero-badge">
                    <span class="badge-icon">🏢</span>
                    <span>Verificadores de la Calidad</span>
                </div>
                <h1>Dando <span class="gradient-text">Certeza</span> al Mercado en la Era Digital</h1>
                <p class="hero-description">
                    El mundo giró y las viejas prácticas comerciales ya no son suficientes. En VQS, nacimos para cerrar la brecha entre las promesas y la realidad, asegurando que cada producto y servicio cumpla con las expectativas del cliente moderno.
                </p>
                <div class="hero-metrics">
                    <div class="metric-item">
                        <div class="metric-number">1,200+</div>
                        <div class="metric-label">Clientes Verificados</div>
                    </div>
                    <div class="metric-item">
                        <div class="metric-number">4</div>
                        <div class="metric-label">Áreas de Servicio</div>
                    </div>
                    <div class="metric-item">
                        <div class="metric-number">98%</div>
                        <div class="metric-label">Satisfacción de Cliente</div>
                    </div>
                    <div class="metric-item">
                        <div class="metric-number">100%</div>
                        <div class="metric-label">Compromiso con la Certeza</div>
                    </div>
                </div>
            </div>
            <div class="hero-visual">
                <div class="company-showcase">
                    <div class="showcase-header">
                        <div class="company-logo">VQS</div>
                        <div class="company-status">
                            <div class="status-indicator"></div>
                            <span>Verificando Confianza</span>
                        </div>
                    </div>
                    <div class="showcase-content">
                        <div class="global-presence">
                            <h4>Nuestros Pilares</h4>
                            <div class="presence-stats">
                                <div class="presence-item">
                                    <span class="presence-icon">⏱️</span>
                                    <span>Puntualidad</span>
                                </div>
                                <div class="presence-item">
                                    <span class="presence-icon">⭐</span>
                                    <span>Calidad</span>
                                </div>
                                <div class="presence-item">
                                    <span class="presence-icon">💰</span>
                                    <span>Precio Justo</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Mission & Vision Section -->
    <section class="mission-vision-section">
        <div class="section-header fade-in-up">
            <h2>Nuestra Razón de Ser</h2>
            <p>El manifiesto que impulsa cada una de nuestras acciones.</p>
        </div>
        <div class="mission-vision-grid">
            <div class="mission-card fade-in-up stagger-delay-1">
                <div class="card-icon">🎯</div>
                <h3>Misión</h3>
                <p>
                    Verificar el cumplimiento de las expectativas y la satisfacción de los clientes en cada transacción. Somos el aliado que garantiza que las empresas entreguen lo que prometen, restaurando la confianza y la certeza en un mercado que ya no espera.
                </p>
                <div class="mission-pillars">
                    <div class="pillar">
                        <span class="pillar-icon">🔍</span>
                        <span>Verificación Rigurosa</span>
                    </div>
                    <div class="pillar">
                        <span class="pillar-icon">🤝</span>
                        <span>Confianza del Cliente</span>
                    </div>
                    <div class="pillar">
                        <span class="pillar-icon">📈</span>
                        <span>Mejora Continua</span>
                    </div>
                </div>
            </div>
            <div class="vision-card fade-in-up stagger-delay-2">
                <div class="card-icon">🔮</div>
                <h3>Visión</h3>
                <p>
                    Crear un estándar global de certeza donde cada consumidor tenga el poder de saber que lo que adquiere es real y de calidad. Aspiramos a un futuro donde ninguna empresa pueda permitirse no cumplir, porque VQS estará allí para verificarlo.
                </p>
                <div class="vision-goals">
                    <div class="goal">
                        <span class="goal-icon">🏆</span>
                        <span>Estándar de Mercado</span>
                    </div>
                    <div class="goal">
                        <span class="goal-icon">🌍</span>
                        <span>Impacto Global</span>
                    </div>
                    <div class="goal">
                        <span class="goal-icon">💡</span>
                        <span>Innovación Constante</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Timeline Section -->
    <section class="timeline-section">
        <div class="section-header fade-in-up">
            <h2>Nuestra Historia</h2>
            <p>Hitos en nuestro camino para devolver la certeza al mercado.</p>
        </div>
        <div class="timeline">
            {milestones.map((milestone, index) => (
                <div class={`timeline-item fade-in-up stagger-delay-${(index % 3) + 1}`}>
                    <div class="timeline-marker">
                        <div class="marker-icon">{milestone.icon}</div>
                        <div class="marker-year">{milestone.year}</div>
                    </div>
                    <div class="timeline-content">
                        <h3>{milestone.title}</h3>
                        <p>{milestone.description}</p>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <!-- Team Section -->
    <section class="team-section">
        <div class="section-header fade-in-up">
            <h2>Nuestro Equipo Directivo</h2>
            <p>Expertos apasionados por la calidad, la tecnología y el cumplimiento.</p>
        </div>
        <div class="team-grid">
            {teamMembers.map((member, index) => (
                <div class={`team-card fade-in-up stagger-delay-${(index % 4) + 1}`}>
                    <div class="member-avatar">
                        <img src={member.image} alt={member.name} />
                    </div>
                    <div class="member-info">
                        <h3>{member.name}</h3>
                        <div class="member-position">{member.position}</div>
                        <div class="member-specialty">{member.specialty}</div>
                        <div class="member-experience">
                            <span class="experience-icon">⏱️</span>
                            <span>{member.experience} de experiencia</span>
                        </div>
                        <div class="member-education">{member.education}</div>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <!-- Principles Section -->
    <section class="certifications-section">
        <div class="section-header fade-in-up">
            <h2>Nuestros Principios de Verificación</h2>
            <p>Los pilares inquebrantables que definen nuestro servicio.</p>
        </div>
        <div class="certifications-grid">
            {principles.map((cert, index) => (
                <div class={`cert-card fade-in-up stagger-delay-${(index % 3) + 1}`}>
                    <div class="cert-icon">{cert.icon}</div>
                    <h3>{cert.name}</h3>
                    <p>{cert.description}</p>
                    <div class="cert-status">
                        <span class="status-dot"></span>
                        <span>Principio Activo</span>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <!-- Technology Innovation Section -->
    <section class="innovation-section">
        <div class="innovation-background">
            <div class="innovation-pattern"></div>
        </div>
        <div class="section-header fade-in-up">
            <h2>Tecnología para la Certeza</h2>
            <p>Investigación y desarrollo continuo para una verificación infalible.</p>
        </div>
        <div class="innovation-content">
            <div class="innovation-stats fade-in-up">
                <div class="innovation-stat">
                    <div class="stat-icon">📡</div>
                    <div class="stat-number">10k+</div>
                    <div class="stat-label">Sensores Activos</div>
                </div>
                <div class="innovation-stat">
                    <div class="stat-icon">🛡️</div>
                    <div class="stat-number">99.9%</div>
                    <div class="stat-label">Seguridad de Datos</div>
                </div>
                <div class="innovation-stat">
                    <div class="stat-icon">📊</div>
                    <div class="stat-number">5M+</div>
                    <div class="stat-label">Encuestas Procesadas</div>
                </div>
                <div class="innovation-stat">
                    <div class="stat-icon">⚙️</div>
                    <div class="stat-number">20k+</div>
                    <div class="stat-label">Equipos Verificados</div>
                </div>
            </div>
            <div class="innovation-features fade-in-up">
                <div class="feature-item">
                    <div class="feature-icon">🧠</div>
                    <div class="feature-content">
                        <h4>Análisis Predictivo</h4>
                        <p>Algoritmos que anticipan fallos en la calidad y el servicio antes de que ocurran.</p>
                    </div>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">🔗</div>
                    <div class="feature-content">
                        <h4>Trazabilidad Transparente</h4>
                        <p>Sistemas que mapean cada paso de la cadena de valor para una transparencia total.</p>
                    </div>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">📈</div>
                    <div class="feature-content">
                        <h4>Plataformas de Datos</h4>
                        <p>Dashboards intuitivos para visualizar la satisfacción del cliente en tiempo real.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Values Section -->
    <section class="values-section">
        <div class="section-header fade-in-up">
            <h2>Nuestros Valores</h2>
            <p>Principios que guían nuestro compromiso con la certeza.</p>
        </div>
        <div class="values-grid">
            <div class="value-card fade-in-up stagger-delay-1">
                <div class="value-icon">🎯</div>
                <h3>Certeza</h3>
                <p>Es la piedra angular de todo lo que hacemos. Luchamos para que sea un derecho, no un lujo.</p>
            </div>
            <div class="value-card fade-in-up stagger-delay-2">
                <div class="value-icon">🤝</div>
                <h3>Integridad</h3>
                <p>Actuamos con honestidad y transparencia. Nuestra palabra es nuestra garantía.</p>
            </div>
            <div class="value-card fade-in-up stagger-delay-3">
                <div class="value-icon">💡</div>
                <h3>Innovación</h3>
                <p>El mundo no se detiene, y nosotros tampoco. Innovamos para estar siempre un paso adelante.</p>
            </div>
            <div class="value-card fade-in-up stagger-delay-4">
                <div class="value-icon">📈</div>
                <h3>Foco en el Cliente</h3>
                <p>Entendemos y defendemos las expectativas del cliente final como si fueran las nuestras.</p>
            </div>
            <div class="value-card fade-in-up stagger-delay-1">
                <div class="value-icon">⚖️</div>
                <h3>Imparcialidad</h3>
                <p>Somos un tercero neutral y objetivo. Nuestros datos y verificaciones son nuestra única voz.</p>
            </div>
            <div class="value-card fade-in-up stagger-delay-2">
                <div class="value-icon">🌍</div>
                <h3>Responsabilidad</h3>
                <p>Asumimos la responsabilidad de fortalecer la confianza en el mercado global.</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-background">
            <div class="cta-glow"></div>
        </div>
        <div class="cta-content fade-in-up">
            <div class="cta-text">
                <h2>¿Su empresa está lista para el nuevo mundo?</h2>
                <p>No se quede atrás. Deje que VQS verifique su promesa y la convierta en la confianza que fideliza a sus clientes.</p>
            </div>
            <div class="cta-actions">
                <a href="/servicios" class="btn-primary large">
                    <span>Conocer los Servicios</span>
                    <span class="btn-icon">💼</span>
                </a>
                <a href="/contacto" class="btn-secondary large">
                    <span class="btn-icon">📧</span>
                    <span>Contactar a un Experto</span>
                </a>
            </div>
        </div>
    </section>
</MainLayout>

<style>
    /* Hero Section */
    .about-hero {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-xl) calc(-1 * var(--space-xl)) var(--space-4xl);
        background: var(--gradient-primary);
        color: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
    }

    .hero-background {
        position: absolute;
        inset: 0;
        opacity: 0.1;
    }

    .hero-pattern {
        position: absolute;
        inset: 0;
        background-image: 
            linear-gradient(30deg, rgba(255,255,255,0.1) 12%, transparent 12.5%, transparent 87%, rgba(255,255,255,0.1) 87.5%, rgba(255,255,255,0.1)),
            linear-gradient(150deg, rgba(255,255,255,0.1) 12%, transparent 12.5%, transparent 87%, rgba(255,255,255,0.1) 87.5%, rgba(255,255,255,0.1));
        background-size: 80px 80px;
        animation: pattern-slide 25s linear infinite;
    }

    .hero-glow {
        position: absolute;
        top: 10%;
        right: 20%;
        width: 400px;
        height: 400px;
        background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
        border-radius: 50%;
        animation: float 10s ease-in-out infinite;
    }

    @keyframes pattern-slide {
        0% { transform: translate(0, 0); }
        100% { transform: translate(80px, 80px); }
    }

    .hero-content {
        position: relative;
        z-index: 2;
        display: grid;
        grid-template-columns: 1.3fr 0.7fr;
        gap: var(--space-4xl);
        align-items: center;
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
    }

    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        padding: var(--space-sm) var(--space-lg);
        border-radius: var(--radius-full);
        font-size: 0.9rem;
        font-weight: 500;
        margin-bottom: var(--space-xl);
    }

    .hero-content h1 {
        font-size: 3.25rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: var(--space-xl);
    }

    .hero-description {
        font-size: 1.2rem;
        line-height: 1.6;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        max-width: 600px;
    }

    .hero-metrics {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: var(--space-lg);
    }

    .metric-item {
        text-align: center;
        padding: var(--space-lg);
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: var(--radius-xl);
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .metric-number {
        font-size: 2rem;
        font-weight: 800;
        color: #FFD700;
        margin-bottom: var(--space-xs);
    }

    .metric-label {
        font-size: 0.85rem;
        opacity: 0.8;
        font-weight: 500;
    }

    /* Company Showcase */
    .company-showcase {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: var(--radius-2xl);
        padding: var(--space-xl);
        box-shadow: var(--shadow-2xl);
        animation: float 8s ease-in-out infinite;
    }

    .showcase-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--space-lg);
    }

    .company-logo {
        font-size: 1.5rem;
        font-weight: 800;
        color: white;
    }

    .company-status {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        font-size: 0.85rem;
        opacity: 0.8;
    }

    .status-indicator {
        width: 8px;
        height: 8px;
        background: #00FF88;
        border-radius: 50%;
        animation: pulse 2s ease-in-out infinite;
    }

    .global-presence h4 {
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: var(--space-lg);
        color: white;
    }

    .presence-stats {
        display: flex;
        flex-direction: column;
        gap: var(--space-md);
    }

    .presence-item {
        display: flex;
        align-items: center;
        gap: var(--space-md);
        padding: var(--space-sm) var(--space-md);
        background: rgba(255, 255, 255, 0.1);
        border-radius: var(--radius-lg);
        font-size: 0.9rem;
    }

    .presence-icon {
        font-size: 1.2rem;
    }

    /* Mission & Vision Section */
    .mission-vision-section {
        padding: var(--space-4xl) 0;
    }

    .mission-vision-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-3xl);
    }

    .mission-card, .vision-card {
        background: white;
        padding: var(--space-3xl);
        border-radius: var(--radius-2xl);
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .mission-card:hover, .vision-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-2xl);
    }

    .card-icon {
        font-size: 3rem;
        margin-bottom: var(--space-lg);
    }

    .mission-card h3, .vision-card h3 {
        font-size: 1.75rem;
        font-weight: 700;
        color: var(--primary-blue);
        margin-bottom: var(--space-lg);
    }

    .mission-card p, .vision-card p {
        font-size: 1.1rem;
        line-height: 1.6;
        color: var(--neutral-700);
        margin-bottom: var(--space-xl);
    }

    .mission-pillars, .vision-goals {
        display: flex;
        flex-direction: column;
        gap: var(--space-md);
    }

    .pillar, .goal {
        display: flex;
        align-items: center;
        gap: var(--space-md);
        padding: var(--space-md);
        background: var(--neutral-50);
        border-radius: var(--radius-lg);
        font-weight: 500;
        color: var(--neutral-700);
    }

    .pillar-icon, .goal-icon {
        font-size: 1.2rem;
    }

    /* Timeline Section */
    .timeline-section {
        padding: var(--space-4xl) 0;
        background: var(--gradient-surface);
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        border-radius: var(--radius-2xl);
    }

    .timeline {
        max-width: 1000px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
        position: relative;
    }

    .timeline::before {
        content: '';
        position: absolute;
        left: 50%;
        top: 0;
        bottom: 0;
        width: 2px;
        background: var(--gradient-primary);
        transform: translateX(-50%);
    }

    .timeline-item {
        display: flex;
        align-items: center;
        margin-bottom: var(--space-3xl);
        position: relative;
    }

    .timeline-item:nth-child(even) {
        flex-direction: row-reverse;
    }

    .timeline-marker {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--space-sm);
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        z-index: 2;
    }

    .marker-icon {
        width: 60px;
        height: 60px;
        background: var(--gradient-primary);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        box-shadow: var(--shadow-lg);
    }

    .marker-year {
        background: white;
        color: var(--primary-blue);
        padding: var(--space-xs) var(--space-md);
        border-radius: var(--radius-full);
        font-weight: 700;
        font-size: 0.9rem;
        box-shadow: var(--shadow-md);
        border: 2px solid var(--primary-blue);
    }

    .timeline-content {
        background: white;
        padding: var(--space-xl);
        border-radius: var(--radius-xl);
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
        width: calc(50% - 60px);
        margin: 0 var(--space-2xl);
    }

    .timeline-item:nth-child(even) .timeline-content {
        margin: 0 var(--space-2xl) 0 0;
    }

    .timeline-item:nth-child(odd) .timeline-content {
        margin: 0 0 0 var(--space-2xl);
    }

    .timeline-content h3 {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--primary-blue);
        margin-bottom: var(--space-md);
    }

    .timeline-content p {
        color: var(--neutral-700);
        line-height: 1.6;
    }

    /* Team Section */
    .team-section {
        padding: var(--space-4xl) 0;
    }

    .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: var(--space-2xl);
    }

    .team-card {
        background: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .team-card:hover {
        transform: translateY(-12px);
        box-shadow: var(--shadow-2xl);
    }

    .member-avatar {
        position: relative;
        height: 280px;
        overflow: hidden;
    }

    .member-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }

    .team-card:hover .member-avatar img {
        transform: scale(1.05);
    }

    .member-overlay {
        position: absolute;
        top: var(--space-lg);
        right: var(--space-lg);
        background: rgba(0, 0, 0, 0.8);
        color: white;
        padding: var(--space-sm) var(--space-md);
        border-radius: var(--radius-lg);
        font-size: 0.8rem;
        font-weight: 600;
    }

    .member-info {
        padding: var(--space-xl);
    }

    .member-info h3 {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--neutral-800);
        margin-bottom: var(--space-sm);
    }

    .member-position {
        color: var(--primary-blue);
        font-weight: 600;
        font-size: 1rem;
        margin-bottom: var(--space-sm);
    }

    .member-specialty {
        color: var(--neutral-600);
        font-size: 0.9rem;
        margin-bottom: var(--space-lg);
    }

    .member-experience {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        color: var(--neutral-700);
        font-size: 0.9rem;
        font-weight: 500;
    }

    .experience-icon {
        font-size: 1rem;
    }

    /* Certifications Section */
    .certifications-section {
        padding: var(--space-4xl) 0;
        background: var(--primary-navy);
        color: white;
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        border-radius: var(--radius-2xl);
    }

    .certifications-section .section-header h2 {
        color: white;
    }

    .certifications-section .section-header p {
        color: rgba(255, 255, 255, 0.8);
    }

    .certifications-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: var(--space-xl);
        padding: 0 var(--space-xl);
    }

    .cert-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: var(--radius-xl);
        padding: var(--space-xl);
        text-align: center;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .cert-card:hover {
        transform: translateY(-8px);
        background: rgba(255, 255, 255, 0.15);
        box-shadow: var(--shadow-2xl);
    }

    .cert-icon {
        font-size: 3rem;
        margin-bottom: var(--space-lg);
    }

    .cert-card h3 {
        font-size: 1.2rem;
        font-weight: 700;
        color: white;
        margin-bottom: var(--space-md);
    }

    .cert-card p {
        color: rgba(255, 255, 255, 0.8);
        line-height: 1.5;
        margin-bottom: var(--space-lg);
    }

    .cert-status {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: var(--space-sm);
        font-size: 0.85rem;
        color: #00FF88;
        font-weight: 500;
    }

    .status-dot {
        width: 6px;
        height: 6px;
        background: #00FF88;
        border-radius: 50%;
        animation: pulse 2s ease-in-out infinite;
    }

    /* Innovation Section */
    .innovation-section {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        background: var(--gradient-surface);
        border-radius: var(--radius-2xl);
        overflow: hidden;
    }

    .innovation-background {
        position: absolute;
        inset: 0;
        opacity: 0.05;
    }

    .innovation-pattern {
        position: absolute;
        inset: 0;
        background-image: 
            radial-gradient(circle at 20% 50%, rgba(30, 64, 175, 0.3) 0%, transparent 50%),
            radial-gradient(circle at 80% 20%, rgba(5, 150, 105, 0.3) 0%, transparent 50%),
            radial-gradient(circle at 40% 80%, rgba(217, 119, 6, 0.3) 0%, transparent 50%);
        animation: innovation-flow 20s ease-in-out infinite;
    }

    @keyframes innovation-flow {
        0%, 100% { transform: translate(0, 0) scale(1); }
        33% { transform: translate(-20px, -20px) scale(1.1); }
        66% { transform: translate(20px, -10px) scale(0.9); }
    }

    .innovation-content {
        position: relative;
        z-index: 2;
        padding: 0 var(--space-xl);
    }

    .innovation-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: var(--space-xl);
        margin-bottom: var(--space-4xl);
    }

    .innovation-stat {
        background: white;
        padding: var(--space-xl);
        border-radius: var(--radius-xl);
        text-align: center;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
        transition: all 0.3s ease;
    }

    .innovation-stat:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-xl);
    }

    .innovation-stat .stat-icon {
        font-size: 2.5rem;
        margin-bottom: var(--space-md);
    }

    .innovation-stat .stat-number {
        font-size: 2rem;
        font-weight: 800;
        color: var(--primary-blue);
        margin-bottom: var(--space-xs);
    }

    .innovation-stat .stat-label {
        color: var(--neutral-600);
        font-weight: 500;
    }

    .innovation-features {
        display: flex;
        flex-direction: column;
        gap: var(--space-xl);
        max-width: 800px;
        margin: 0 auto;
    }

    .feature-item {
        display: flex;
        align-items: flex-start;
        gap: var(--space-xl);
        background: white;
        padding: var(--space-xl);
        border-radius: var(--radius-xl);
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
    }

    .feature-icon {
        font-size: 2.5rem;
        flex-shrink: 0;
    }

    .feature-content h4 {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--neutral-800);
        margin-bottom: var(--space-md);
    }

    .feature-content p {
        color: var(--neutral-600);
        line-height: 1.6;
    }

    /* Values Section */
    .values-section {
        padding: var(--space-4xl) 0;
    }

    .values-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: var(--space-xl);
    }

    .value-card {
        background: white;
        padding: var(--space-2xl);
        border-radius: var(--radius-xl);
        text-align: center;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--neutral-200);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .value-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-2xl);
    }

    .value-icon {
        font-size: 3rem;
        margin-bottom: var(--space-lg);
    }

    .value-card h3 {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--primary-blue);
        margin-bottom: var(--space-md);
    }

    .value-card p {
        color: var(--neutral-700);
        line-height: 1.6;
    }

    /* CTA Section */
    .cta-section {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        background: var(--gradient-accent);
        color: white;
        border-radius: var(--radius-2xl);
        text-align: center;
        overflow: hidden;
    }

    .cta-background {
        position: absolute;
        inset: 0;
    }

    .cta-glow {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 50%);
        animation: rotate 25s linear infinite;
    }

    @keyframes rotate {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }

    .cta-content {
        position: relative;
        z-index: 2;
        max-width: 800px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
    }

    .cta-text h2 {
        font-size: 2.75rem;
        font-weight: 800;
        margin-bottom: var(--space-lg);
    }

    .cta-text p {
        font-size: 1.2rem;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        line-height: 1.6;
    }

    .cta-actions {
        display: flex;
        gap: var(--space-lg);
        justify-content: center;
        flex-wrap: wrap;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .hero-content {
            grid-template-columns: 1fr;
            gap: var(--space-2xl);
            text-align: center;
        }

        .hero-metrics {
            grid-template-columns: repeat(2, 1fr);
        }

        .mission-vision-grid {
            grid-template-columns: 1fr;
            gap: var(--space-2xl);
        }

        .timeline::before {
            left: 30px;
        }

        .timeline-marker {
            left: 30px;
        }

        .timeline-content {
            width: calc(100% - 100px);
            margin: 0 0 0 var(--space-3xl);
        }

        .timeline-item:nth-child(even) .timeline-content {
            margin: 0 0 0 var(--space-3xl);
        }
    }

    @media (max-width: 768px) {
        .hero-content h1 {
            font-size: 2.5rem;
        }

        .hero-metrics {
            grid-template-columns: 1fr 1fr;
            gap: var(--space-md);
        }

        .section-header h2 {
            font-size: 2.25rem;
        }

        .team-grid {
            grid-template-columns: 1fr;
        }

        .certifications-grid {
            grid-template-columns: 1fr;
            padding: 0 var(--space-md);
        }

        .innovation-stats {
            grid-template-columns: 1fr 1fr;
        }

        .innovation-features {
            padding: 0 var(--space-md);
        }

        .feature-item {
            flex-direction: column;
            text-align: center;
            gap: var(--space-lg);
        }

        .values-grid {
            grid-template-columns: 1fr;
        }

        .cta-text h2 {
            font-size: 2.25rem;
        }

        .cta-actions {
            flex-direction: column;
            align-items: center;
        }
    }
</style>

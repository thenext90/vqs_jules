---
import MainLayout from '../layouts/MainLayout.astro';

const serviceAreas = [
    {
        name: 'Alimentación',
        description: 'Seguridad alimenticia en línea y sensores de análisis para garantizar la calidad y el cumplimiento normativo en toda la cadena de producción.',
        icon: '🍎',
        color: '#22C55E',
        bgColor: 'rgba(34, 197, 94, 0.1)',
    },
    {
        name: 'Información y Soporte',
        description: 'Máxima seguridad y protección de datos para sus sistemas, asegurando la confidencialidad e integridad de la información crítica de su negocio.',
        icon: '💻',
        color: '#3B82F6',
        bgColor: 'rgba(59, 130, 246, 0.1)',
    },
    {
        name: 'Encuestas y Estadísticas',
        description: 'Medición precisa de la satisfacción de clientes y gestión de no conformidades para una mejora continua de sus servicios y productos.',
        icon: '📊',
        color: '#F97316',
        bgColor: 'rgba(249, 115, 22, 0.1)',
    },
    {
        name: 'Mantenimiento y Equipos',
        description: 'Verificación del cumplimiento de las regulaciones legales y normativas en todas las actividades de mantenimiento y uso de equipamientos.',
        icon: '🔧',
        color: '#6B7280',
        bgColor: 'rgba(107, 114, 128, 0.1)',
    }
];
---

<MainLayout title="VQS - Servicio de Verificación de la Calidad">
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-background">
            <div class="hero-grid"></div>
            <div class="hero-glow"></div>
        </div>
        <div class="hero-content">
            <div class="hero-text fade-in-up">
                <div class="hero-badge">
                    <span class="badge-icon">✅</span>
                    <span>Servicio de Verificación de Calidad</span>
                </div>
                <h1 class="hero-title">
                    Garantizamos la Certeza
                    <span class="gradient-text">de lo que Compras y Recibes</span>
                </h1>
                <p class="hero-description">
                    En un mundo que no se detiene, VQS verifica el cumplimiento de las expectativas y la satisfacción de los clientes. Porque tener la certeza de que lo que se adquiere es lo que se recibe, es un derecho.
                </p>
                <div class="hero-stats">
                    <div class="stat-item fade-in-up stagger-delay-1">
                        <div class="stat-number">100%</div>
                        <div class="stat-label">Compromiso</div>
                    </div>
                    <div class="stat-item fade-in-up stagger-delay-2">
                        <div class="stat-number">4</div>
                        <div class="stat-label">Áreas de Servicio</div>
                    </div>
                    <div class="stat-item fade-in-up stagger-delay-3">
                        <div class="stat-number">24/7</div>
                        <div class="stat-label">Soporte Activo</div>
                    </div>
                    <div class="stat-item fade-in-up stagger-delay-4">
                        <div class="stat-number">1,200+</div>
                        <div class="stat-label">Clientes Satisfechos</div>
                    </div>
                </div>
                <div class="hero-actions fade-in-up">
                    <a href="#servicios" class="btn-primary">
                        <span>Nuestros Servicios</span>
                        <span class="btn-icon">→</span>
                    </a>
                    <a href="/contacto" class="btn-secondary">
                        <span class="btn-icon">▶</span>
                        <span>Solicitar Verificación</span>
                    </a>
                </div>
            </div>
            <div class="hero-visual fade-in-up">
                <div class="certification-preview">
                    <div class="preview-header">
                        <div class="preview-logo">VQS</div>
                        <div class="preview-status">
                            <div class="status-dot"></div>
                            <span>Verificando...</span>
                        </div>
                    </div>
                    <div class="preview-content">
                        <div class="preview-product">
                            <div class="product-image-preview" style="background: linear-gradient(135deg, #3B82F6, #10B981);"></div>
                            <div class="product-info">
                                <h4>Verificación Multidimensional</h4>
                                <div class="analysis-bars">
                                    <div class="analysis-bar">
                                        <span>Cumplimiento</span>
                                        <div class="bar"><div class="fill" style="width: 99%"></div></div>
                                        <span>99%</span>
                                    </div>
                                    <div class="analysis-bar">
                                        <span>Calidad</span>
                                        <div class="bar"><div class="fill" style="width: 97%"></div></div>
                                        <span>97%</span>
                                    </div>
                                    <div class="analysis-bar">
                                        <span>Satisfacción</span>
                                        <div class="bar"><div class="fill" style="width: 98%"></div></div>
                                        <span>98%</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Service Areas Section -->
    <section class="certification-levels-section" id="servicios">
        <div class="section-header fade-in-up">
            <h2>Áreas de Verificación VQS</h2>
            <p>Soluciones integrales para garantizar la calidad en los sectores más exigentes</p>
        </div>
        <div class="levels-grid">
            {serviceAreas.map((area, index) => (
                <div class={`level-card fade-in-up stagger-delay-${index + 1}`}>
                    <div class="level-header" style={`background: ${area.bgColor}; border-color: ${area.color}`}>
                        <div class="level-badge" style={`background: ${area.color}; font-size: 2rem; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center;`}>
                            {area.icon}
                        </div>
                        <h3 style={`color: ${area.color}; margin-top: 1rem;`}>{area.name}</h3>
                    </div>
                    <div class="level-content">
                        <p style="color: var(--neutral-700); min-height: 100px;">{area.description}</p>
                        <div class="level-features">
                            <div class="feature-item">
                                <span class="feature-icon">✓</span>
                                <span>Protocolos ISO y BRC</span>
                            </div>
                            <div class="feature-item">
                                <span class="feature-icon">✓</span>
                                <span>Normas Sectoriales HACCP</span>
                            </div>
                            <div class="feature-item">
                                <span class="feature-icon">✓</span>
                                <span>Satisfacción del Cliente</span>
                            </div>
                        </div>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <!-- Technology Section -->
    <section class="technology-section">
        <div class="tech-background">
            <div class="tech-pattern"></div>
        </div>
        <div class="section-header fade-in-up">
            <h2>Tecnología para la Certeza</h2>
            <p>Infraestructura robusta para una verificación confiable y transparente</p>
        </div>
        
        <div class="tech-grid">
            <div class="tech-card fade-in-up stagger-delay-1">
                <div class="tech-icon">📡</div>
                <h3>Sensores y Análisis en Línea</h3>
                <p>Monitorización en tiempo real para sectores como alimentación y mantenimiento, garantizando el cumplimiento de los estándares de calidad y seguridad.</p>
                <div class="tech-stats">
                    <div class="tech-stat">
                        <span class="stat-number">24/7</span>
                        <span class="stat-label">Monitoreo</span>
                    </div>
                    <div class="tech-stat">
                        <span class="stat-number">100+</span>
                        <span class="stat-label">Variables</span>
                    </div>
                </div>
            </div>
            
            <div class="tech-card fade-in-up stagger-delay-2">
                <div class="tech-icon">🛡️</div>
                <h3>Seguridad y Protección de Datos</h3>
                <p>Plataformas seguras que protegen la integridad y confidencialidad de la información, cumpliendo con las regulaciones más estrictas de protección de datos.</p>
                <div class="tech-stats">
                    <div class="tech-stat">
                        <span class="stat-number">99.9%</span>
                        <span class="stat-label">Disponibilidad</span>
                    </div>
                    <div class="tech-stat">
                        <span class="stat-number">Nivel 5</span>
                        <span class="stat-label">Seguridad</span>
                    </div>
                </div>
            </div>
            
            <div class="tech-card fade-in-up stagger-delay-3">
                <div class="tech-icon">📈</div>
                <h3>Análisis Estadístico Avanzado</h3>
                <p>Herramientas de análisis de datos para procesar encuestas y métricas de satisfacción, identificando áreas de mejora y tendencias del mercado.</p>
                <div class="tech-stats">
                    <div class="tech-stat">
                        <span class="stat-number">1M+</span>
                        <span class="stat-label">Datos Procesados</span>
                    </div>
                    <div class="tech-stat">
                        <span class="stat-number">98%</span>
                        <span class="stat-label">Precisión</span>
                    </div>
                </div>
            </div>
            
            <div class="tech-card fade-in-up stagger-delay-4">
                <div class="tech-icon">🔗</div>
                <h3>Trazabilidad Integral</h3>
                <p>Sistemas que aseguran la trazabilidad de cada proceso, desde el origen hasta el cliente final, aportando transparencia y confianza.</p>
                <div class="tech-stats">
                    <div class="tech-stat">
                        <span class="stat-number">100%</span>
                        <span class="stat-label">Transparencia</span>
                    </div>
                    <div class="tech-stat">
                        <span class="stat-number">Inmutable</span>
                        <span class="stat-label">Registro</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-background">
            <div class="cta-glow"></div>
        </div>
        <div class="cta-content fade-in-up">
            <div class="cta-text">
                <h2>¿Listo para dar Certeza a sus Clientes?</h2>
                <p>Únase a las empresas que ya están construyendo confianza y lealtad a través de la verificación de calidad de VQS.</p>
            </div>
            <div class="cta-actions">
                <a href="/contacto" class="btn-primary large">
                    <span>Contactar a un Experto</span>
                    <span class="btn-icon">🚀</span>
                </a>
                <a href="/servicios" class="btn-secondary large">
                    <span class="btn-icon">📞</span>
                    <span>Conocer Más</span>
                </a>
            </div>
            <div class="cta-features">
                <div class="cta-feature">
                    <span class="feature-icon">⚡</span>
                    <span>Implementación Rápida</span>
                </div>
                <div class="cta-feature">
                    <span class="feature-icon">🛡️</span>
                    <span>Garantía de Cumplimiento</span>
                </div>
                <div class="cta-feature">
                    <span class="feature-icon">📊</span>
                    <span>Informes de Impacto</span>
                </div>
            </div>
        </div>
    </section>
</MainLayout>

<style>
    /* Hero Section */
    .hero-section {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-xl) calc(-1 * var(--space-xl)) var(--space-4xl);
        overflow: hidden;
        background: var(--gradient-primary);
        border-radius: var(--radius-2xl);
    }

    .hero-background {
        position: absolute;
        inset: 0;
        opacity: 0.1;
    }

    .hero-grid {
        position: absolute;
        inset: 0;
        background-image: 
            linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px),
            linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px);
        background-size: 50px 50px;
        animation: grid-move 20s linear infinite;
    }

    .hero-glow {
        position: absolute;
        top: 20%;
        right: 10%;
        width: 300px;
        height: 300px;
        background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, transparent 70%);
        border-radius: 50%;
        animation: float 8s ease-in-out infinite;
    }

    @keyframes grid-move {
        0% { transform: translate(0, 0); }
        100% { transform: translate(50px, 50px); }
    }

    .hero-content {
        position: relative;
        z-index: 2;
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        gap: var(--space-4xl);
        align-items: center;
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
        color: white;
    }

    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        padding: var(--space-sm) var(--space-lg);
        border-radius: var(--radius-full);
        font-size: 0.9rem;
        font-weight: 500;
        margin-bottom: var(--space-xl);
    }

    .badge-icon {
        font-size: 1.2rem;
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: var(--space-xl);
    }

    .hero-description {
        font-size: 1.25rem;
        line-height: 1.6;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        max-width: 600px;
    }

    .hero-stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: var(--space-xl);
        margin-bottom: var(--space-2xl);
    }

    .stat-item {
        text-align: center;
    }

    .stat-number {
        display: block;
        font-size: 2.5rem;
        font-weight: 800;
        color: #FFD700;
        margin-bottom: var(--space-xs);
    }

    .stat-label {
        font-size: 0.9rem;
        opacity: 0.8;
        font-weight: 500;
    }

    .hero-actions {
        display: flex;
        gap: var(--space-lg);
        align-items: center;
    }

    .btn-primary, .btn-secondary {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        padding: var(--space-lg) var(--space-2xl);
        border-radius: var(--radius-full);
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
    }

    .btn-primary {
        background: white;
        color: var(--primary-navy);
        box-shadow: var(--shadow-lg);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-xl);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.1);
        color: white;
        border: 1px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(10px);
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
    }

    .btn-primary.large, .btn-secondary.large {
        padding: var(--space-xl) var(--space-3xl);
        font-size: 1.1rem;
    }

    .btn-icon {
        font-size: 1.2rem;
    }

    /* Certification Preview */
    .certification-preview {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: var(--radius-2xl);
        padding: var(--space-xl);
        box-shadow: var(--shadow-2xl);
        animation: float 6s ease-in-out infinite;
    }

    .preview-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--space-lg);
    }

    .preview-logo {
        font-size: 1.5rem;
        font-weight: 800;
        color: white;
    }

    .preview-status {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        font-size: 0.9rem;
        opacity: 0.8;
    }

    .status-dot {
        width: 8px;
        height: 8px;
        background: #00FF88;
        border-radius: 50%;
        animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.5; transform: scale(1.2); }
    }

    .product-image-preview {
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, #FF6B6B, #4ECDC4);
        border-radius: var(--radius-lg);
        margin-bottom: var(--space-lg);
    }

    .product-info h4 {
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: var(--space-lg);
        color: white;
    }

    .analysis-bars {
        display: flex;
        flex-direction: column;
        gap: var(--space-md);
    }

    .analysis-bar {
        display: flex;
        align-items: center;
        gap: var(--space-md);
        font-size: 0.85rem;
    }

    .analysis-bar span:first-child {
        width: 80px;
        opacity: 0.8;
    }

    .analysis-bar span:last-child {
        width: 40px;
        text-align: right;
        font-weight: 600;
    }

    .bar {
        flex: 1;
        height: 6px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 3px;
        overflow: hidden;
    }

    .fill {
        height: 100%;
        background: linear-gradient(90deg, #00FF88, #00D4FF);
        border-radius: 3px;
        animation: fill-animation 2s ease-out;
    }

    @keyframes fill-animation {
        0% { width: 0%; }
    }

    /* Certification Levels Section */
    .certification-levels-section {
        padding: var(--space-4xl) 0;
    }

    .section-header {
        text-align: center;
        margin-bottom: var(--space-4xl);
    }

    .section-header h2 {
        font-size: 2.75rem;
        font-weight: 800;
        color: var(--neutral-800);
        margin-bottom: var(--space-lg);
    }

    .section-header p {
        font-size: 1.2rem;
        color: var(--neutral-600);
        max-width: 600px;
        margin: 0 auto;
    }

    .levels-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: var(--space-xl);
    }

    .level-card {
        background: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
        box-shadow: var(--shadow-lg);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid var(--neutral-200);
    }

    .level-card:hover {
        transform: translateY(-8px);
        box-shadow: var(--shadow-2xl);
    }

    .level-header {
        padding: var(--space-xl);
        border-bottom: 1px solid;
        text-align: center;
    }

    .level-badge {
        display: inline-block;
        color: white;
        padding: var(--space-sm) var(--space-lg);
        border-radius: var(--radius-full);
        font-weight: 700;
        font-size: 0.9rem;
        margin-bottom: var(--space-lg);
        letter-spacing: 0.5px;
    }

    .level-header h3 {
        font-size: 1.3rem;
        font-weight: 600;
        margin: 0;
    }

    .level-content {
        padding: var(--space-xl);
    }

    .level-score {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--space-xl);
        padding: var(--space-lg);
        background: var(--neutral-50);
        border-radius: var(--radius-lg);
    }

    .score-label {
        font-size: 0.9rem;
        color: var(--neutral-600);
        font-weight: 500;
    }

    .score-value {
        font-size: 1.5rem;
        font-weight: 700;
    }

    .level-features {
        display: flex;
        flex-direction: column;
        gap: var(--space-md);
    }

    .feature-item {
        display: flex;
        align-items: center;
        gap: var(--space-md);
        font-size: 0.95rem;
        color: var(--neutral-700);
    }

    .feature-item.premium {
        color: var(--accent-gold);
        font-weight: 600;
    }

    .feature-icon {
        width: 20px;
        height: 20px;
        background: var(--accent-emerald);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.8rem;
        flex-shrink: 0;
    }

    .feature-item.premium .feature-icon {
        background: var(--accent-gold);
    }

    /* Products Section */
    .products-section {
        padding: var(--space-4xl) 0;
        background: var(--gradient-surface);
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        border-radius: var(--radius-2xl);
    }

    .filter-section {
        margin-bottom: var(--space-3xl);
    }

    .filter-controls {
        display: flex;
        justify-content: center;
        gap: var(--space-md);
        flex-wrap: wrap;
    }

    .filter-btn {
        background: white;
        border: 2px solid var(--neutral-200);
        color: var(--neutral-700);
        padding: var(--space-md) var(--space-xl);
        border-radius: var(--radius-full);
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        font-size: 0.95rem;
    }

    .filter-btn:hover, .filter-btn.active {
        background: var(--primary-blue);
        border-color: var(--primary-blue);
        color: white;
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
        gap: var(--space-2xl);
        padding: 0 var(--space-xl);
    }

    .product-card {
        background: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
        box-shadow: var(--shadow-lg);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid var(--neutral-200);
    }

    .product-card:hover {
        transform: translateY(-12px);
        box-shadow: var(--shadow-2xl);
    }

    .product-image {
        position: relative;
        height: 220px;
        overflow: hidden;
    }

    .product-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }

    .product-card:hover .product-image img {
        transform: scale(1.05);
    }

    .certification-overlay {
        position: absolute;
        top: var(--space-lg);
        left: var(--space-lg);
        right: var(--space-lg);
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
    }

    .cert-badge {
        color: white;
        padding: var(--space-sm) var(--space-lg);
        border-radius: var(--radius-full);
        font-weight: 700;
        font-size: 0.85rem;
        box-shadow: var(--shadow-md);
        letter-spacing: 0.5px;
    }

    .ai-score {
        background: rgba(0, 0, 0, 0.8);
        backdrop-filter: blur(10px);
        color: white;
        padding: var(--space-sm) var(--space-md);
        border-radius: var(--radius-lg);
        text-align: center;
        box-shadow: var(--shadow-md);
    }

    .ai-score .score-label {
        display: block;
        font-size: 0.7rem;
        opacity: 0.8;
        margin-bottom: 2px;
    }

    .ai-score .score-value {
        display: block;
        font-size: 1.1rem;
        font-weight: 700;
        color: #00FF88;
    }

    .product-content {
        padding: var(--space-xl);
    }

    .product-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--space-md);
    }

    .product-category {
        color: var(--primary-blue);
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .batch-id {
        font-family: 'JetBrains Mono', monospace;
        font-size: 0.8rem;
        color: var(--neutral-500);
        background: var(--neutral-100);
        padding: var(--space-xs) var(--space-sm);
        border-radius: var(--radius-sm);
    }

    .product-name {
        font-size: 1.3rem;
        font-weight: 700;
        color: var(--neutral-800);
        margin-bottom: var(--space-lg);
        line-height: 1.3;
    }

    .certification-info {
        margin-bottom: var(--space-lg);
    }

    .cert-level {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        padding: var(--space-sm) var(--space-md);
        border-radius: var(--radius-lg);
        font-weight: 600;
        font-size: 0.9rem;
    }

    .cert-icon {
        font-size: 1rem;
    }

    .product-features {
        display: flex;
        flex-wrap: wrap;
        gap: var(--space-sm);
        margin-bottom: var(--space-lg);
    }

    .feature-tag {
        background: var(--neutral-100);
        color: var(--neutral-700);
        padding: var(--space-xs) var(--space-md);
        border-radius: var(--radius-full);
        font-size: 0.8rem;
        font-weight: 500;
    }

    .certification-dates {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-md);
        margin-bottom: var(--space-lg);
        padding: var(--space-md);
        background: var(--neutral-50);
        border-radius: var(--radius-lg);
    }

    .date-item {
        text-align: center;
    }

    .date-label {
        display: block;
        font-size: 0.8rem;
        color: var(--neutral-500);
        margin-bottom: var(--space-xs);
    }

    .date-value {
        display: block;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--neutral-800);
        font-family: 'JetBrains Mono', monospace;
    }

    .product-actions {
        display: flex;
        gap: var(--space-md);
    }

    .btn-view-cert {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: var(--space-sm);
        background: var(--gradient-primary);
        color: white;
        border: none;
        padding: var(--space-md) var(--space-lg);
        border-radius: var(--radius-lg);
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .btn-view-cert:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .btn-trace {
        background: var(--neutral-100);
        color: var(--neutral-700);
        border: none;
        padding: var(--space-md);
        border-radius: var(--radius-lg);
        cursor: pointer;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .btn-trace:hover {
        background: var(--neutral-200);
        transform: translateY(-2px);
    }

    /* Technology Section */
    .technology-section {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        background: var(--primary-navy);
        color: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
    }

    .tech-background {
        position: absolute;
        inset: 0;
        opacity: 0.1;
    }

    .tech-pattern {
        position: absolute;
        inset: 0;
        background-image: radial-gradient(circle at 25% 25%, rgba(255,255,255,0.1) 2px, transparent 2px);
        background-size: 40px 40px;
        animation: pattern-move 15s linear infinite;
    }

    @keyframes pattern-move {
        0% { transform: translate(0, 0); }
        100% { transform: translate(40px, 40px); }
    }

    .technology-section .section-header {
        position: relative;
        z-index: 2;
    }

    .technology-section .section-header h2 {
        color: white;
    }

    .technology-section .section-header p {
        color: rgba(255, 255, 255, 0.8);
    }

    .tech-grid {
        position: relative;
        z-index: 2;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: var(--space-2xl);
        padding: 0 var(--space-xl);
    }

    .tech-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: var(--radius-2xl);
        padding: var(--space-2xl);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .tech-card:hover {
        transform: translateY(-8px);
        background: rgba(255, 255, 255, 0.15);
        box-shadow: var(--shadow-2xl);
    }

    .tech-card .tech-icon {
        font-size: 3rem;
        margin-bottom: var(--space-lg);
        display: block;
    }

    .tech-card h3 {
        font-size: 1.4rem;
        font-weight: 700;
        margin-bottom: var(--space-lg);
        color: white;
    }

    .tech-card p {
        color: rgba(255, 255, 255, 0.8);
        line-height: 1.6;
        margin-bottom: var(--space-xl);
    }

    .tech-stats {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--space-lg);
    }

    .tech-stat {
        text-align: center;
        padding: var(--space-md);
        background: rgba(255, 255, 255, 0.1);
        border-radius: var(--radius-lg);
    }

    .tech-stat .stat-number {
        display: block;
        font-size: 1.5rem;
        font-weight: 700;
        color: #00FF88;
        margin-bottom: var(--space-xs);
    }

    .tech-stat .stat-label {
        font-size: 0.8rem;
        color: rgba(255, 255, 255, 0.7);
    }

    /* CTA Section */
    .cta-section {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-4xl) calc(-1 * var(--space-xl)) 0;
        background: var(--gradient-accent);
        color: white;
        border-radius: var(--radius-2xl);
        text-align: center;
        overflow: hidden;
    }

    .cta-background {
        position: absolute;
        inset: 0;
    }

    .cta-glow {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 50%);
        animation: rotate 20s linear infinite;
    }

    @keyframes rotate {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }

    .cta-content {
        position: relative;
        z-index: 2;
        max-width: 800px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
    }

    .cta-text h2 {
        font-size: 2.75rem;
        font-weight: 800;
        margin-bottom: var(--space-lg);
    }

    .cta-text p {
        font-size: 1.2rem;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        line-height: 1.6;
    }

    .cta-actions {
        display: flex;
        gap: var(--space-lg);
        justify-content: center;
        margin-bottom: var(--space-2xl);
        flex-wrap: wrap;
    }

    .cta-features {
        display: flex;
        justify-content: center;
        gap: var(--space-2xl);
        flex-wrap: wrap;
    }

    .cta-feature {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        font-size: 0.95rem;
        opacity: 0.9;
    }

    .cta-feature .feature-icon {
        font-size: 1.2rem;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .hero-content {
            grid-template-columns: 1fr;
            gap: var(--space-2xl);
            text-align: center;
        }

        .hero-stats {
            grid-template-columns: repeat(2, 1fr);
        }

        .products-grid {
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            padding: 0 var(--space-lg);
        }
    }

    @media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }

        .hero-stats {
            grid-template-columns: 1fr 1fr;
            gap: var(--space-lg);
        }

        .hero-actions {
            flex-direction: column;
            align-items: stretch;
        }

        .section-header h2 {
            font-size: 2.25rem;
        }

        .levels-grid {
            grid-template-columns: 1fr;
        }

        .products-grid {
            grid-template-columns: 1fr;
            padding: 0 var(--space-md);
        }

        .tech-grid {
            grid-template-columns: 1fr;
            padding: 0 var(--space-md);
        }

        .cta-text h2 {
            font-size: 2.25rem;
        }

        .cta-actions {
            flex-direction: column;
            align-items: stretch;
        }

        .cta-features {
            flex-direction: column;
            align-items: center;
            gap: var(--space-lg);
        }
    }
</style>

<script>
    // Smooth scrolling for anchor links
    document.addEventListener('DOMContentLoaded', function() {
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    });
</script>
---
import MainLayout from '../layouts/MainLayout.astro';

const services = [
    {
        id: 'alimentacion',
        title: 'Verificación de Alimentos',
        description: 'Garantizamos la seguridad y calidad en toda la cadena alimentaria mediante sensores en línea y análisis de datos avanzados.',
        icon: '🍎',
        features: [
            'Monitoreo de seguridad en tiempo real',
            'Análisis de composición y calidad',
            'Cumplimiento de normativas (ISO, BRC, HACCP)',
            'Trazabilidad de lotes de producción',
            'Alertas tempranas de contaminación'
        ],
        benefits: [
            'Asegura la confianza del consumidor',
            'Reduce riesgos y retiradas de productos',
            'Optimiza la calidad de producción',
            'Cumplimiento normativo garantizado'
        ],
        price: 'Desde $199/mes',
        popular: true
    },
    {
        id: 'informacion-soporte',
        title: 'Seguridad de la Información',
        description: 'Protegemos los activos de datos de su empresa, garantizando la confidencialidad, integridad y disponibilidad de la información.',
        icon: '💻',
        features: [
            'Auditorías de seguridad de sistemas',
            'Análisis de vulnerabilidades',
            'Planes de protección de datos (GDPR, etc.)',
            'Verificación de infraestructura de TI',
            'Capacitación en ciberseguridad'
        ],
        benefits: [
            'Protege contra brechas de datos',
            'Asegura la continuidad del negocio',
            'Genera confianza en clientes y socios',
            'Cumple con regulaciones de privacidad'
        ],
        price: 'Desde $299/mes',
        popular: false
    },
    {
        id: 'encuestas-estadisticas',
        title: 'Análisis de Satisfacción',
        description: 'Medimos y analizamos la percepción de sus clientes para identificar oportunidades de mejora y gestionar no conformidades.',
        icon: '📊',
        features: [
            'Diseño e implementación de encuestas',
            'Análisis estadístico de resultados',
            'Identificación de puntos críticos',
            'Gestión de no conformidades',
            'Informes de inteligencia de cliente'
        ],
        benefits: [
            'Mejora la lealtad del cliente',
            'Toma de decisiones basada en datos',
            'Optimización de productos y servicios',
            'Respuesta ágil a problemas'
        ],
        price: 'Desde $149/mes',
        popular: false
    },
    {
        id: 'mantenimiento-equipos',
        title: 'Verificación de Mantenimiento',
        description: 'Aseguramos que las actividades de mantenimiento y los equipos cumplan con las regulaciones legales y normativas vigentes.',
        icon: '🔧',
        features: [
            'Auditoría de planes de mantenimiento',
            'Verificación de cumplimiento normativo',
            'Inspección de seguridad de equipos',
            'Certificación de procesos',
            'Análisis de fiabilidad y vida útil'
        ],
        benefits: [
            'Garantiza la seguridad operacional',
            'Previene fallos y accidentes',
            'Asegura el cumplimiento legal',
            'Optimiza la gestión de activos'
        ],
        price: 'Desde $249/mes',
        popular: false
    }
];

const processSteps = [
    {
        step: 1,
        title: 'Diagnóstico Inicial',
        description: 'Realizamos un análisis exhaustivo de sus procesos y necesidades para definir el alcance de la verificación.',
        icon: '📝',
        duration: '1-2 días'
    },
    {
        step: 2,
        title: 'Diseño del Plan',
        description: 'Creamos un plan de verificación a medida, estableciendo protocolos, métricas y KPIs específicos.',
        icon: '📋',
        duration: '2-3 días'
    },
    {
        step: 3,
        title: 'Implementación',
        description: 'Desplegamos nuestras herramientas y sensores, y comenzamos el monitoreo y la recopilación de datos.',
        icon: '🚀',
        duration: 'Depende del proyecto'
    },
    {
        step: 4,
        title: 'Análisis y Reporte',
        description: 'Nuestros expertos analizan los datos y generan informes detallados con hallazgos y recomendaciones.',
        icon: '🔬',
        duration: '3-5 días'
    },
    {
        step: 5,
        title: 'Emisión de Verificación',
        description: 'Emitimos el informe final de verificación que certifica el cumplimiento de los estándares establecidos.',
        icon: '🏆',
        duration: '1 día'
    },
    {
        step: 6,
        title: 'Mejora Continua',
        description: 'Ofrecemos seguimiento y soporte para asegurar la mejora continua y el mantenimiento de la calidad.',
        icon: '✅',
        duration: 'Continuo'
    }
];
---

<MainLayout title="VQS - Servicios de Verificación de Calidad">
    <!-- Hero Section -->
    <section class="services-hero">
        <div class="hero-background">
            <div class="hero-pattern"></div>
            <div class="hero-glow"></div>
        </div>
        <div class="hero-content fade-in-up">
            <div class="hero-badge">
                <span class="badge-icon">✅</span>
                <span>Soluciones Integrales de Calidad</span>
            </div>
            <h1>Nuestros Servicios de <span class="gradient-text">Verificación</span></h1>
            <p class="hero-description">
                Ofrecemos un portafolio completo de servicios diseñados para dar certeza y confianza a su empresa y sus clientes. Verificamos que sus productos, servicios y procesos cumplan con las más altas expectativas de calidad y cumplimiento.
            </p>
            <div class="hero-stats">
                <div class="stat-item">
                    <div class="stat-number">4</div>
                    <div class="stat-label">Áreas de Especialización</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">100%</div>
                    <div class="stat-label">Foco en el Cliente</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">24/7</div>
                    <div class="stat-label">Monitoreo y Soporte</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Services Grid -->
    <section class="services-section">
        <div class="section-header fade-in-up">
            <h2>Explore Nuestras Áreas de Servicio</h2>
            <p>Soluciones expertas para los desafíos de calidad más exigentes de la industria moderna.</p>
        </div>
        <div class="services-grid">
            {services.map((service, index) => (
                <div class={`service-card ${service.popular ? 'popular' : ''} fade-in-up stagger-delay-${(index % 4) + 1}`}>
                    {service.popular && <div class="popular-badge">Más Popular</div>}
                    <div class="service-header">
                        <div class="service-icon">{service.icon}</div>
                        <h3>{service.title}</h3>
                        <p class="service-description">{service.description}</p>
                    </div>
                    <div class="service-features">
                        <h4>Incluye:</h4>
                        <ul>
                            {service.features.map(feature => (
                                <li>
                                    <span class="feature-icon">✓</span>
                                    {feature}
                                </li>
                            ))}
                        </ul>
                    </div>
                    <div class="service-benefits">
                        <h4>Resultados:</h4>
                        <ul>
                            {service.benefits.map(benefit => (
                                <li>
                                    <span class="benefit-icon">🎯</span>
                                    {benefit}
                                </li>
                            ))}
                        </ul>
                    </div>
                    <div class="service-footer">
                        <div class="service-price">{service.price}</div>
                        <a href="/contacto" class="btn-primary service-btn">
                            <span>Cotizar</span>
                            <span class="btn-icon">→</span>
                        </a>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <!-- Process Section -->
    <section class="process-section">
        <div class="process-background">
            <div class="process-pattern"></div>
        </div>
        <div class="section-header fade-in-up">
            <h2>Nuestro Proceso de Verificación</h2>
            <p>Un flujo de trabajo transparente y eficiente para garantizar resultados confiables.</p>
        </div>
        <div class="process-timeline">
            {processSteps.map((step, index) => (
                <div class={`process-step fade-in-up stagger-delay-${(index % 6) + 1}`}>
                    <div class="step-marker">
                        <div class="step-number">{step.step}</div>
                        <div class="step-icon">{step.icon}</div>
                    </div>
                    <div class="step-content">
                        <h3>{step.title}</h3>
                        <p>{step.description}</p>
                        <div class="step-duration">⏱️ {step.duration}</div>
                    </div>
                    {index < processSteps.length - 1 && <div class="step-connector"></div>}
                </div>
            ))}
        </div>
        <div class="process-summary fade-in-up">
            <div class="summary-card">
                <div class="summary-icon">🤝</div>
                <h3>Alianza Estratégica</h3>
                <div class="total-time">Más que un servicio, una asociación</div>
                <p>Trabajamos con usted para construir una cultura de calidad y certeza.</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="services-cta-section">
        <div class="cta-background">
            <div class="cta-glow"></div>
        </div>
        <div class="cta-content fade-in-up">
            <h2>Dé el siguiente paso hacia la certeza total.</h2>
            <p>Contacte a nuestros expertos para un diagnóstico sin costo y descubra cómo VQS puede transformar la calidad en su organización.</p>
            <div class="cta-actions">
                <a href="/contacto" class="btn-primary large">
                    <span>Solicitar Diagnóstico</span>
                    <span class="btn-icon">🚀</span>
                </a>
                <a href="/nosotros" class="btn-secondary large">
                    <span class="btn-icon">💬</span>
                    <span>Conocer más de VQS</span>
                </a>
            </div>
            <div class="cta-benefits">
                <div class="benefit-item">
                    <span class="benefit-icon">✅</span>
                    <span>Diagnóstico sin compromiso</span>
                </div>
                <div class="benefit-item">
                    <span class="benefit-icon">🔒</span>
                    <span>100% Confidencialidad</span>
                </div>
                <div class="benefit-item">
                    <span class="benefit-icon">⚡</span>
                    <span>Respuesta en menos de 24h</span>
                </div>
            </div>
        </div>
    </section>
</MainLayout>

<style>
    /* Hero Section */
    .services-hero {
        position: relative;
        padding: var(--space-4xl) 0;
        margin: var(--space-xl) calc(-1 * var(--space-xl)) var(--space-4xl);
        background: var(--gradient-primary);
        color: white;
        border-radius: var(--radius-2xl);
        overflow: hidden;
    }

    .hero-background {
        position: absolute;
        inset: 0;
        opacity: 0.1;
    }

    .hero-pattern {
        position: absolute;
        inset: 0;
        background-image: 
            linear-gradient(30deg, rgba(255,255,255,0.1) 12%, transparent 12.5%, transparent 87%, rgba(255,255,255,0.1) 87.5%),
            linear-gradient(150deg, rgba(255,255,255,0.1) 12%, transparent 12.5%, transparent 87%, rgba(255,255,255,0.1) 87.5%);
        background-size: 80px 80px;
        animation: pattern-slide 25s linear infinite;
    }

    .hero-glow {
        position: absolute;
        top: 20%;
        right: 10%;
        width: 300px;
        height: 300px;
        background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
        border-radius: 50%;
        animation: float 8s ease-in-out infinite;
    }

    @keyframes pattern-slide {
        0% { transform: translate(0, 0); }
        100% { transform: translate(80px, 80px); }
    }

    .hero-content {
        position: relative;
        z-index: 2;
        text-align: center;
        max-width: 900px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
    }

    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        padding: var(--space-sm) var(--space-lg);
        border-radius: var(--radius-full);
        font-size: 0.9rem;
        font-weight: 500;
        margin-bottom: var(--space-xl);
    }

    .services-hero h1 {
        font-size: 3.5rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: var(--space-xl);
    }

    .hero-description {
        font-size: 1.2rem;
        line-height: 1.6;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
    }

    .hero-stats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: var(--space-xl);
        max-width: 600px;
        margin: 0 auto;
    }

    .stat-item {
        text-align: center;
    }

    .stat-number {
        font-size: 2.5rem;
        font-weight: 800;
        color: #FFD700;
        margin-bottom: var(--space-xs);
    }

    .stat-label {
        font-size: 0.9rem;
        opacity: 0.8;
        font-weight: 500;
    }

    /* Services Section */
    .services-section {
        padding: var(--space-4xl) 0;
    }
    .section-header {
        text-align: center;
        margin-bottom: var(--space-4xl);
    }

    .section-header h2 {
        font-size: 2.75rem;
        font-weight: 800;
        color: var(--neutral-800);
        margin-bottom: var(--space-lg);
    }

    .section-header p {
        font-size: 1.2rem;
        color: var(--neutral-600);
        max-width: 600px;
        margin: 0 auto;
    }
    .services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: var(--space-2xl);
    }

    .service-card {
        background: white;
        border-radius: var(--radius-xl);
        padding: var(--space-2xl);
        box-shadow: var(--shadow-lg);
        border: 2px solid transparent;
        transition: all var(--transition-normal);
        position: relative;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .service-card:hover {
        transform: translateY(-5px);
        box-shadow: var(--shadow-2xl);
        border-color: var(--primary-blue);
    }

    .service-card.popular {
        border-color: var(--primary-blue);
        box-shadow: var(--shadow-2xl);
    }

    .popular-badge {
        position: absolute;
        top: var(--space-lg);
        right: var(--space-lg);
        background: var(--gradient-primary);
        color: white;
        padding: var(--space-xs) var(--space-md);
        border-radius: var(--radius-full);
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .service-header {
        margin-bottom: var(--space-lg);
    }

    .service-icon {
        font-size: 3rem;
        margin-bottom: var(--space-lg);
    }

    .service-card h3 {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--neutral-800);
        margin-bottom: var(--space-md);
    }

    .service-description {
        color: var(--neutral-600);
        line-height: 1.6;
        margin-bottom: var(--space-lg);
        flex-grow: 1;
    }

    .service-features, .service-benefits {
        margin-bottom: var(--space-xl);
    }

    .service-features h4, .service-benefits h4 {
        font-size: 1rem;
        font-weight: 600;
        color: var(--neutral-800);
        margin-bottom: var(--space-md);
    }

    .service-features ul, .service-benefits ul {
        list-style: none;
        padding: 0;
    }

    .service-features li, .service-benefits li {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        padding: var(--space-xs) 0;
        color: var(--neutral-700);
        font-size: 0.9rem;
    }

    .feature-icon {
        color: var(--primary-blue);
        font-weight: 600;
        width: 16px;
        text-align: center;
    }

    .benefit-icon {
        font-size: 0.8rem;
    }

    .service-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: var(--space-lg);
        border-top: 1px solid var(--neutral-200);
        margin-top: auto;
    }

    .service-price {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--primary-blue);
    }

    .service-btn {
        padding: var(--space-md) var(--space-xl);
        font-size: 0.9rem;
    }

    /* Process Section */
    .process-section {
        padding: var(--space-4xl) 0;
        background: var(--neutral-50);
        position: relative;
        overflow: hidden;
    }

    .process-background {
        position: absolute;
        inset: 0;
        opacity: 0.05;
    }

    .process-pattern {
        position: absolute;
        inset: 0;
        background-image: 
            radial-gradient(circle at 25% 25%, var(--primary-blue) 2px, transparent 2px),
            radial-gradient(circle at 75% 75%, var(--primary-blue) 1px, transparent 1px);
        background-size: 50px 50px;
        animation: pattern-move 20s linear infinite;
    }

    @keyframes pattern-move {
        0% { transform: translate(0, 0); }
        100% { transform: translate(50px, 50px); }
    }

    .process-timeline {
        position: relative;
        max-width: 1000px;
        margin: 0 auto;
        padding: var(--space-2xl) 0;
    }

    .process-step {
        display: flex;
        align-items: center;
        gap: var(--space-2xl);
        margin-bottom: var(--space-3xl);
        position: relative;
    }

    .process-step:last-child {
        margin-bottom: 0;
    }

    .step-marker {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--space-md);
        min-width: 100px;
    }

    .step-number {
        width: 40px;
        height: 40px;
        background: var(--gradient-primary);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 1.1rem;
    }

    .step-icon {
        font-size: 2rem;
    }

    .step-content {
        background: white;
        padding: var(--space-xl);
        border-radius: var(--radius-xl);
        box-shadow: var(--shadow-lg);
        flex: 1;
        border: 1px solid var(--neutral-200);
    }

    .step-content h3 {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--neutral-900);
        margin-bottom: var(--space-md);
    }

    .step-content p {
        color: var(--neutral-600);
        line-height: 1.6;
        margin-bottom: var(--space-md);
    }

    .step-duration {
        display: inline-flex;
        align-items: center;
        gap: var(--space-xs);
        background: var(--neutral-100);
        color: var(--neutral-800);
        padding: var(--space-xs) var(--space-md);
        border-radius: var(--radius-full);
        font-size: 0.85rem;
        font-weight: 500;
    }

    .step-connector {
        position: absolute;
        left: 69px;
        top: 100px;
        width: 2px;
        height: 60px;
        background: var(--gradient-primary);
        z-index: -1;
    }

    .process-summary {
        text-align: center;
        margin-top: var(--space-3xl);
    }

    .summary-card {
        background: white;
        padding: var(--space-2xl);
        border-radius: var(--radius-xl);
        box-shadow: var(--shadow-xl);
        display: inline-block;
        border: 2px solid var(--primary-blue);
    }

    .summary-icon {
        font-size: 3rem;
        margin-bottom: var(--space-md);
    }

    .summary-card h3 {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--neutral-900);
        margin-bottom: var(--space-md);
    }

    .total-time {
        font-size: 2rem;
        font-weight: 800;
        color: var(--primary-blue);
        margin-bottom: var(--space-md);
    }

    /* CTA Section */
    .services-cta-section {
        padding: var(--space-4xl) 0;
        background: var(--gradient-accent);
        color: white;
        position: relative;
        overflow: hidden;
    }

    .cta-background {
        position: absolute;
        inset: 0;
    }

    .cta-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        animation: float 12s ease-in-out infinite;
    }

    .cta-content {
        position: relative;
        z-index: 2;
        text-align: center;
        max-width: 700px;
        margin: 0 auto;
        padding: 0 var(--space-xl);
    }

    .services-cta-section h2 {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: var(--space-lg);
    }

    .services-cta-section p {
        font-size: 1.2rem;
        opacity: 0.9;
        margin-bottom: var(--space-2xl);
        line-height: 1.6;
    }

    .cta-actions {
        display: flex;
        justify-content: center;
        gap: var(--space-lg);
        margin-bottom: var(--space-2xl);
        flex-wrap: wrap;
    }

    .cta-benefits {
        display: flex;
        justify-content: center;
        gap: var(--space-xl);
        flex-wrap: wrap;
    }

    .benefit-item {
        display: flex;
        align-items: center;
        gap: var(--space-sm);
        font-size: 0.9rem;
        opacity: 0.9;
    }

    .benefit-icon {
        font-size: 1.1rem;
    }
    .btn-primary, .btn-secondary {
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
        padding: var(--space-lg) var(--space-2xl);
        border-radius: var(--radius-full);
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
    }

    .btn-primary {
        background: white;
        color: var(--primary-navy);
        box-shadow: var(--shadow-lg);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-xl);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.1);
        color: white;
        border: 1px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(10px);
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
    }

    .btn-primary.large, .btn-secondary.large {
        padding: var(--space-xl) var(--space-3xl);
        font-size: 1.1rem;
    }

    .btn-icon {
        font-size: 1.2rem;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .services-hero h1 {
            font-size: 2.5rem;
        }

        .hero-stats {
            grid-template-columns: 1fr;
            gap: var(--space-lg);
        }

        .services-grid {
            grid-template-columns: 1fr;
        }

        .certification-grid {
            grid-template-columns: 1fr;
        }

        .industries-grid {
            grid-template-columns: 1fr;
        }

        .process-step {
            flex-direction: column;
            text-align: center;
        }

        .step-connector {
            display: none;
        }

        .cta-actions {
            flex-direction: column;
            align-items: stretch;
        }

        .cta-benefits {
            flex-direction: column;
            align-items: center;
            gap: var(--space-md);
        }
    }

    /* Animation Classes */
    .fade-in-up {
        opacity: 0;
        transform: translateY(30px);
        animation: fadeInUp 0.8s ease-out forwards;
    }

    .stagger-delay-1 { animation-delay: 0.1s; }
    .stagger-delay-2 { animation-delay: 0.2s; }
    .stagger-delay-3 { animation-delay: 0.3s; }
    .stagger-delay-4 { animation-delay: 0.4s; }
    .stagger-delay-5 { animation-delay: 0.5s; }
    .stagger-delay-6 { animation-delay: 0.6s; }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.6; }
    }
</style>